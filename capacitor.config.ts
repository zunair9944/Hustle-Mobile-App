import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Hustle_CaseJohnson',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
